<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
    # => app/views/users/リソース名/_モデル名.html.erb
    # => app/views/users/_user.html.erb
  <% end %>
</ul>

<%= will_paginate %>